spring:
  data:
    redis:
      host: redis
      port: 6379
      database: 0

rag2agent:
  storage:
    postgres:
      host: postgres
      port: 5432
      database: ${POSTGRES_DB:rag2agent}
    minio:
      endpoint: http://minio:9000
      bucket: rag2agent-documents
