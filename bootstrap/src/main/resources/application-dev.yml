spring:
  data:
    redis:
      host: localhost
      port: 6379
      database: 0

rag2agent:
  storage:
    postgres:
      host: localhost
      port: 5432
      database: rag2agent
    minio:
      endpoint: http://localhost:9000
      bucket: rag2agent-documents
