name: rag2agent

services:
  postgres:
    image: pgvector/pgvector:pg17
    container_name: rag2agent-postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-rag2agent}
      POSTGRES_USER: ${POSTGRES_USER:-rag2agent}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-rag2agent}
    ports:
      - "15432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./docker/postgres/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-rag2agent} -d ${POSTGRES_DB:-rag2agent}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7.4-alpine
    container_name: rag2agent-redis
    ports:
      - "16379:6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  minio:
    image: minio/minio:RELEASE.2025-09-07T16-13-09Z
    container_name: rag2agent-minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-rag2agent}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-rag2agent-password}
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio-data:/data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 10s
      timeout: 5s
      retries: 5

  rocketmq-namesrv:
    image: apache/rocketmq:5.3.2
    container_name: rag2agent-rocketmq-namesrv
    command: ["nameserver"]
    environment:
      JAVA_OPT_EXT: "-server -Xms512m -Xmx512m -Xmn256m"
    ports:
      - "19876:9876"
    volumes:
      - rocketmq-namesrv-logs:/root/logs
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'echo > /dev/tcp/127.0.0.1/9876' 2>/dev/null || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5

  rocketmq-broker:
    image: apache/rocketmq:5.3.2
    container_name: rag2agent-rocketmq-broker
    depends_on:
      rocketmq-namesrv:
        condition: service_healthy
    command: ["broker", "-n", "rocketmq-namesrv:9876", "-c", "/home/rocketmq/rocketmq-5.3.2/conf/broker.conf"]
    environment:
      # 开发机 Docker 内存有限，保持 broker 可启动并不影响生产环境参数。
      JAVA_OPT_EXT: "-server -Xms256m -Xmx256m -Xmn128m"
      NAMESRV_ADDR: rocketmq-namesrv:9876
    ports:
      # Windows 动态保留端口区会占用 RocketMQ 默认宿主端口，容器内端口保持不变。
      - "19091:19091"
      - "19111:19111"
      - "19112:19112"
    volumes:
      - ./docker/rocketmq/broker.conf:/home/rocketmq/rocketmq-5.3.2/conf/broker.conf:ro
      - rocketmq-broker-logs:/root/logs
      - rocketmq-broker-store:/root/store
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'echo > /dev/tcp/127.0.0.1/19111' 2>/dev/null || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

  neo4j:
    image: neo4j:5-community
    container_name: rag2agent-neo4j
    environment:
      NEO4J_AUTH: neo4j/${NEO4J_PASSWORD:-rag2agent-password}
    ports:
      - "17474:7474"
      - "17687:7687"
    volumes:
      - neo4j-data:/data
    healthcheck:
      test: ["CMD-SHELL", "cypher-shell -u neo4j -p ${NEO4J_PASSWORD:-rag2agent-password} -d neo4j 'RETURN 1' > /dev/null 2>&1 || exit 1"]
      interval: 10s
      timeout: 10s
      retries: 5
      start_period: 30s

  jaeger:
    image: jaegertracing/all-in-one:1.60
    container_name: rag2agent-jaeger
    environment:
      COLLECTOR_OTLP_ENABLED: "true"
    ports:
      - "16686:16686"
      - "4317:4317"
      - "4318:4318"
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O - http://127.0.0.1:14269/ || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres-data:
  redis-data:
  minio-data:
  rocketmq-namesrv-logs:
  rocketmq-broker-logs:
  rocketmq-broker-store:
  neo4j-data:
